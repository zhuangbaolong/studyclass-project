
#微服务配置
spring:
  application:
    name: learning-service
  cloud:
    nacos:
      server-addr: 192.168.88.130:8848
      discovery:
        namespace: dev
        group: studyclass-project
      config:
        namespace: dev
        group: studyclass-project
        file-extension: yml
        refresh-enabled: true
        extension-configs:
          - data-id: learning-service-${spring.profiles.active}.yml
            group: studyclass-project
            refresh: true
        shared-configs:
          - data-id: swagger-${spring.profiles.active}.yml
            group: studyclass-common
            refresh: true
          - data-id: logging-${spring.profiles.active}.yml
            group: studyclass-common
            refresh: true
          - data-id: feign-${spring.profiles.active}.yml
            group: studyclass-common
            refresh: true

  profiles:
    active: dev

